BROKER SCHEMA procs



CREATE PROCEDURE generateComplaintID(IN dummy CHAR) RETURNS CHAR
BEGIN
	DECLARE iv INTERVAL (CURRENT_TIMESTAMP - TIMESTAMP '2005-09-29 00:00:01') SECOND;
	RETURN 'COM' || CAST( (CAST(iv AS INT)) AS CHAR);
END;


